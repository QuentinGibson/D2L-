<p id="notice"><%= notice %></p>
<div class="d-flex ">
  <div class="task-attributes-container">
    <span><%= @task.name %> </span>
    <span><%= @task.description %></span>
    <span><%= @task.due_date %></span>
    <span><%= @task.color %></span>
  </div>
</div>

<%= link_to "Edit", edit_subject_task_path(@subject, @task) %>
